# 文档入口

- [日常运行指南](./日常运行指南.md)：每天启动、选择运行方式、处理恢复待办、暂停与退出，以及常见问题。

- [开发与验收历史记录](./历史/开发与验收记录-2026-09.md)：历史实现、环境快照、测试结果、修复批次和 IN3/N2 验收证据，不作为每日操作说明。
- [股票能力接入与开发顺序](./股票能力接入.md)：聊天底座需要先补什么、如何接股票工具、各阶段验收及两个仓库的职责；当前状态、联调证据与已知缺口见该文第 6 节。
- [回归验证指南](./回归验证指南.md)：聊天与工具案例矩阵、连续对话样例、股票服务启停和独立现金账户恢复回归；只记录验证方法。
- [Harness 能力盘点与差距](./harness能力盘点.md)：把本仓库当 agent harness 评估——已有能力、2026 共识底线（Tier-0/Tier-1）对齐情况、两个结构性观察（含文件预取回归的 git 追溯）与补齐顺序。
- [Harness 能力矩阵 2026](./harness能力矩阵-2026.md)：11 个成熟 harness × 19 个维度的逐工具功能面对照（142 条来源链接），是上文的参考与来源依据。
- [本地 AI Agent 部署指南](../本地AI-Agents部署流程.md)：前置要求、依赖安装、`.env` 配置、唯一启动入口、运维命令与故障排查；只讲部署与运维，不含功能进度。

## 当前状态（2026-09-15 入口统一更新）

| 批次 | 状态 |
| --- | --- |
| 单一入口改造（先于 IN4.16） | ✅ 已实现并自测：唯一入口 `启动.bat`/`scripts/start-daily.ps1`（BFF @9510 同源托管 Web）；页面技能开关（联网搜索 `core/skills_service.py`、股票技能单开关）；退出唯一路径=页面"退出"或关闭页面（15 秒自动全退含容器，`core/auto_exit.py`）；IN1 会话库并入 `data/conversations.db`，旧入口与 legacy 界面（TUI/Ink CLI/gateway）移除；规则见根目录 `AGENTS.md`。 |
| IN4.16 技能结构对齐 | ✅ 已实现并实测（2026-09-15，待人工验收）：Agent Skills（SKILL.md）包零改代码可发现，`skill_list` 渐进披露（清单无正文）、`skill_info` 返回正文+资源清单+注入防护、`skill_run` 引导、wb-finance-skill 端到端可用；容器检查 26 项、离线 132 项全过；详见[股票能力接入](./股票能力接入.md)。 |

| 阶段 | 状态 |
| --- | --- |
| IN1.1～IN1.7 | ✅ 已实现：模型路由与真实来源记录、完整工具协议落库、异常与停止收尾、自然对话与云端数据边界、skills 发现/调用/注册、精确编辑、容器内命令执行。 |
| IN1.H | ✅ 用户经验性验收通过（2026-09-07）。44 个必测案例矩阵与第二段 12 轮对话未逐条全量执行，后续体验问题按反馈随时修底座。 |
| IN2.1～IN2.3 | ✅ 已实现：账户查看、研究查找、报告读取三个只读工具；完整报告只作本地附件，不进模型历史。 |
| IN2.H | ✅ 用户验收通过（2026-09-08，原话“能完整读取，且模型会自己对比”）。 |
| IN3.1～IN3.3 | ✅ 已实现：研究任务提交/取消/恢复，受理≠完成，停止生成与停止研究分离；提交支持按 6 位代码或股票名称二选一（2026-09-09）。 |
| IN3.H | ✅ 用户验收通过（2026-09-14，原话“是的，我已经确认过 IN3.H 的实现了”）；证据与范围见[运行记录](./历史/开发与验收记录-2026-09.md) §10。N2 本批已另行验收；IN4 已登记缺陷保留。 |
| N2.H | ✅ 用户验收通过（2026-09-14），含关闭后台状态修复复测；原 N2 扩大能力仍按统一计划保留。 |
| IN4 | 🔄 进行中：`IN4.18` 已经用户验收、`IN4.17`（板块放开与研究可达性）已完成并端到端实测（2026-09-14，宁德时代 `300750` 与中芯国际 `688981` 研究任务均 `SUCCEEDED`）；**下一批 `IN4.16`（技能结构对齐）**。详见[股票能力接入](./股票能力接入.md)第 3 节与股票仓库 `docs/计划.md`。 |

以下为历史环境快照，不代表 2026-09-14 当前运行状态。

2026-09-10 复核证据：离线测试 `Ran 86 tests` → OK（85 通过 + 1 项容器门控 skip）、`apps/web` 生产构建通过；容器集成检查未重跑（Docker 未运行）；真实模型对话本轮未新增，模型账本 **93/120**，最后一次调用 2026-09-09 15:45 UTC（IN3 模型在环复验）。当前 BFF、Web 与容器均已停止，重启步骤见[本地 AI Agent 部署指南](../本地AI-Agents部署流程.md)。

同日按 IN3.H 前置要求实现名称标识：`stock_research_submit` 支持 `symbol`（6 位代码）或 `name`（股票名称）二选一，名称由股票侧可信证券名称表解析成代码，模型不自行换算；聊天侧提交 `28789dd`，股票侧 `ae91b53`。随后完成 IN3 模型在环复验（真实 Qwen，隔离后台 worker 关闭）：名称提交/复用、跨轮查询、报告读取、定向停止与恢复、步数边界终态六项全部通过，账本 70 → 93；证据见[运行记录](./历史/开发与验收记录-2026-09.md) §7。

本轮同时修正了文档与实现不一致之处：IN1.H/IN2.H 的验收状态、模型调用额度（30 → 120）、工具分层默认值（`core` → `all`）、Agent 轮次上限（六轮 → 12）、股票工具数量（3 → 6）以及若干界面描述。跨仓库完成清单仍以股票仓库 `docs/计划.md` 为唯一来源，该文件已于同日补记 2026-09-07～09 批次、额度变化与名称标识实施。

接入批次使用统一编号 `IN0`～`IN4`。复选框、实施状态、验收证据和提交记录集中维护在股票仓库的 `docs/计划.md`，本仓库接入文档维护本侧步骤与验收要求，不复制完成清单。本机可打开[统一实施进度](D:/Stock_Agent_Workspace/docs/计划.md)。换机器后按仓库名和相对路径查找，盘符不是部署要求。
