# 股票能力接入与开发顺序

> 决策日期：2026-09-06，状态更新：2026-09-15。用户已授权实施，IN1 工程交付与验证见 [开发与验收历史记录](./历史/开发与验收记录-2026-09.md)；IN1.H 已于 2026-09-07 用户经验性验收通过。股票侧工程已推进到 IN3.3（IN2 只读能力、IN3 研究任务链均已实现），IN2.H 于 2026-09-08 验收通过，IN3.H 于 2026-09-14 用户验收通过；IN4.18、IN4.17 已完成（2026-09-14），IN4.16 技能结构对齐已于 2026-09-15 实现并实测通过（待人工验收），下一批 IN4.14/IN4.15。**2026-09-15 起本仓库完成单一入口改造**：唯一启动入口为根目录 `启动.bat`/`scripts/start-daily.ps1`（BFF @9510 同源托管 Web），退出只有页面"退出"或关闭页面（约 15 秒自动全退）；IN1 隔离库已并入 `data/conversations.db`，旧入口（quick-start、start-in1、TUI、Ink CLI）全部移除，规则详见仓库根目录 `AGENTS.md`。当前状态、联调证据与已知缺口见第 6 节。两个仓库都直接在 `main` 开发，不创建额外分支或 worktree。

## 1. 目标与阅读方式

先提供自然、连续的大模型对话，需要时再调用通用工具或股票工作流。普通聊天不进入股票业务流程；业务规则在实际工具执行时校验。本地运行 harness 不要求总控只能使用本地模型。

不等待旧功能规划全部完成，也不一次搬入完整股票系统。顺序为：最小可靠聊天底座 → 三类股票只读能力 → 一条研究任务闭环 → 扩展专业能力、逐项替换 Dify 依赖并补数据面能力。IN4 内部的实际先后见股票仓库 `docs/计划.md` 的「IN4 推荐实施顺序」。

- 本文件：本仓库的实现步骤、模块归属和验收要求。
- 股票仓库 `docs/local-ai-agent接入.md`：后台复用、适配契约、业务边界和迁移顺序；[本机链接](D:/Stock_Agent_Workspace/docs/local-ai-agent接入.md)。
- 股票仓库 `docs/计划.md`：接入任务 `IN0`～`IN4` 的唯一进度表，包括本仓库任务、证据和提交；[本机链接](D:/Stock_Agent_Workspace/docs/计划.md)。本文件不另维护完成复选框。
- [本地 AI Agent 部署指南](../本地AI-Agents部署流程.md)：安装依赖、配置与启动步骤；只讲部署与运维，不含功能进度或接入状态，不能据此判断接入是否已完成。

跨仓库链接中的本机路径仅便于查阅；实现不得硬编码这些路径。

## 2. 现有基础与具体缺口

以下来自 2026-09-06 静态代码检查，不代表已执行新一轮真实聊天验收。

| 模块 | 已有基础 | 本次需要补齐 |
| --- | --- | --- |
| `apps/web`、`bff` | 流式聊天、会话管理、编辑重发、回复版本、文件与工具卡片 | 停止和异常状态一致；中途结果与最终回答分别展示；本地附件独立交付 |
| `core/llm_client.py`、`core/runtime.py` | Ollama `/api/chat` 调用 | 先接一个用户确定的目标供应商和模型；完整适配认证、流式事件、工具调用与错误；保留 Ollama |
| `core/agent.py` | 模型自主选择工具，多轮读取结果后回答 | 默认轮次上限（`AGENT_MAX_ROUNDS`，当前 12）耗尽后的收尾；工具调用元数据；异常、中断与已有结果保留 |
| `bff/service.py`、`core/conversation_store.py` | SQLite 历史与结构化工具结果 | 持久化完整 assistant 工具调用及对应结果，重建历史时保持顺序和关联；旧会话兼容处理 |
| `core/context_manager.py`、`core/memory_manager.py` | 历史压缩与记忆基础 | 压缩不切断工具调用与结果；刷新、编辑、重生成、摘要后的连续语义和数据边界 |
| `core/tool_registry.py`、`core/tool_router.py` | YAML 工具描述和 HTTP 分派 | 新后端地址注册、认证、可信身份、请求编号和结果转换；不能只放 YAML 就视为接通股票后端 |
| Web 模型选择、`bff/service.py`、`core/runtime.py` | 界面已有模型选择控件，后台仍使用固定 Ollama 客户端 | 选中的供应商与模型实际用于本轮推理，回复记录实际模型；保留已配置 Ollama 的选择路径 |
| `skills/runner`、`config/tools/skill_*.yaml`、`code_exec.yaml` | 临时 Python 执行、技能查询、运行与注册已有后端实现 | 核对工具可见性并接通发现、调用和创建复用链；`TOOL_TIER` 默认 `all`（可按 `core`/`extended` 分层收紧），技能运行/注册属于 `extended`，存在代码不等于模型能调用 |
| `config/prompts/modules` | 模块化提示词 | 移除工具后强制“执行步骤／结果／说明”模板；普通聊天自然表达，业务规则交给工具 |

云端接入前必须检查文件预取、工具结果、历史重放、摘要和记忆更新等所有模型输入通路。现有本地文件可读权限不自动等于允许向云端发送正文。模型选型和凭据通过后续本机配置落实，不在文档中记录密钥，也不把当前 Ollama 接口误写成已支持任意云端供应商。

## 3. 分阶段实现

### IN1：最小可靠聊天底座

依赖：用户查阅文档并指示开始实施；落实首个目标模型及可用配置。

1. `IN1.1`：先接一个目标云端供应商/模型并保留 Ollama，Web 列出已配置、已验证可用的模型；选择结果经 BFF 路由到实际推理客户端，回复记录实际供应商/模型，不能只改变显示标签。验证普通流式回复、工具调用及模型不可用时的明确错误，不静默换成其他模型。通用接口允许以后扩展，任意供应商添加、全量模型自动发现和完整管理平台后置。
2. `IN1.2`：统一模型消息、工具调用、工具结果与 UI 事件的对应关系；完整落库并正确重放，保留调用参数、标识和执行状态，兼容现有数据库。
3. `IN1.3`：修复达到调用上限、模型失败、工具失败、断流和停止生成的收尾。工具失败作为可理解的结果交回模型；模型不可用时由界面明确说明，保留已取得内容，不伪装成功。
4. `IN1.4`：调整自然对话提示词，验证上下文压缩、历史和记忆；为云端输入建立显式数据边界。完整本地附件不进入后续历史、摘要或自动记忆的云端输入。
5. `IN1.5`：保留模型自主选择现成工具、临时编写并执行 Python、发现和调用已注册 skill，以及按用户需求创建可复用 skill 的能力。接通 `skill_list`/`skill_info` → `skill_run` 和编写 → `skill_register` → 再次调用的链路，核对 `core`/`extended` 工具分层；模型可见目录与实际可用工具一致，不仅在提示词中声明有技能。沿用现有 runner、执行范围和资源限制，工具结果返回模型供其检查、纠错和继续回答；完整技能管理 UI 后置。
6. `IN1.6`：新增精确编辑能力（工具名称实施时冻结），支持匹配原文后局部替换、冲突/多处匹配检测、返回差异并重新读取核验；文件变化时不盲目覆盖。覆盖中文、换行与编码、长文件局部修改和连续两次修改。
7. `IN1.7`：补齐容器内最小命令执行闭环。明确实际 shell、工作目录、可用程序、超时、退出码和输出截断，支持目录/文本搜索、脚本执行和测试；按执行环境落实工作范围，不只修改提示词或简单删除字符串黑名单。当前执行器是 `/bin/sh`，若选择 Bash 必须安装并明确配置，不冒充 Windows CMD/PowerShell。提供运行状态、增量输出/进度与最终回执，停止或超时后核实进程及子进程状态；首期不做交互式终端和跨重启进程恢复。
8. `IN1.H`：按[回归验证指南](./回归验证指南.md)执行“读 → 改 → 跑 → 检查 → 再改”及多轮体验，用户在 Web 选择已配置模型后自由连续聊天。工具轨迹、输出流、结果事实和自然回答分别验收；未达标先修底座，不扩大股票接入。此阶段可持续使用通用能力，不需要等待 IN2 股票工具。

验证路径：普通聊天 → 用现有工具计算或读合成文件 → 追问 → 纠正 → 换话题 → 回到原话题 → 刷新后接续。另覆盖工具失败、调用上限、编辑重发和停止。定向自动检查用于验证协议、持久化和异常边界，不以通过数量代替自然体验。

IN1 必须额外覆盖以下可观察结果，功能完成状态仍只在统一计划中记录：

| 验收场景 | 通过标准 |
| --- | --- |
| Web 选择目标云端模型或已配置 Ollama 模型 | 实际请求与选择一致；会话/回复保留真实模型信息，刷新可恢复；切换后的新一轮使用新选择，旧回复来源不被改写 |
| 编辑重发或重新生成 | 明确使用的模型，界面与实际请求一致；请求参数不能被固定运行时忽略 |
| 自主完成临时计算 | 用户只表达目标，模型按需要选择 `code_exec`、生成 Python、读取实际结果并解释；代码报错可据回执修正，不虚构运行成功 |
| 发现并调用已有 skill | 合成测试技能可被 `skill_list`/`skill_info` 发现，模型按需调用 `skill_run`，结果进入本轮和后续对话 |
| 保存为可复用 skill | 用户要求“把这个方法保存下来，下次还用”后，模型生成含 `SKILL_METADATA` 和 `run(params)` 的脚本并注册；后续新对话能发现并复用，无需重复粘贴代码 |
| 工具分层或服务不可用 | 配置允许的技能工具实际出现在模型目录；未开放、缺依赖、执行失败等情况有真实回执，不把不可调用工具描述为已执行 |

技能相关测试使用合成文件和测试技能，不操作日常账本。股票专业工作流后续作为同一工具体系的扩展，通用分析和临时 Python 能力继续保留。

完整场景及记录要求集中在[回归验证指南](./回归验证指南.md)，包含模型选择、文件操作、Python、精确编辑、shell、skills、历史、故障和自然长对话。案例是待执行要求，不是测试通过记录。

### IN2：三类股票只读能力

依赖：`IN1.H` 通过。先以独立测试账户、合成报告和可核验状态联调，研究 worker 关闭，不启动研究或修改日常账本。

1. `IN2.1`：与股票后台明确工具契约。先仅开放查看账户、查找已有研究、读取研究报告三类能力。**已冻结（2026-09-07）**：`stock_account_view`（查看账户）、`stock_research_find`（查找已有研究）、`stock_report_read`（读取研究报告）；身份归属、幂等、来源时间、模型观察与本地附件字段、错误映射及可插拔要求见股票仓库 `docs/工具网关.md` 的"IN2：local-ai-agent 只读接入契约"小节（对应提交 `684f804`）。select 会话标的指针本期不纳入；旧 Dify 会话任务在新入口不可见为接受现状。
2. `IN2.2`：增加股票适配模块和工具注册。BFF 注入身份、会话与请求标识；模型只提供业务参数。现有网关使用的票据、序号和安全引用由适配层管理。**已实现（2026-09-07）**：`core/stock_bridge.py` 按轮管理 prepare/step/finish（票据、序号、request_id 不出适配层），本阶段的三个只读工具在未配置 `STOCK_BRIDGE_URL` 时不注册（IN3 又新增三个任务链工具，合计 6 个，门控规则相同）；模型只见白名单投影，F01 完整报告在轮末经 `tool.local_result` 事件回填工具行、Markdown 渲染且不进历史。配置方式见 `.env.example`；隔离联调（不启动 worker、独立 state 目录、零模型调用）验证了真实网关握手、账户投影、空查找、非法引用引导与附件收割。模型在环验收留待 IN2.3/H 与新预算。
3. `IN2.3`：模型读取授权事实与摘要，UI 直接展示完整本地报告；后续追问仍遵守相同边界。研究价格包含证据时间，账面成本不当作市值。**已实现（2026-09-07）**：账户卡单独渲染（状态/现金/持仓数，并提示"账面成本非市值"），研究/报告卡渲染任务/裁决/数据状态徽章与证据时间；泄漏验证以离线测试固化（跨轮历史、上下文压缩、跨会话工具均不含本地报告正文）；合成验收环境为股票仓库 `scripts/seed_bridge_demo.py`（幂等，合成任务带 `SYNTHETIC_DEMO_NOT_FOR_CLOUD` 标记供人工核验边界）。
4. `IN2.H`：用户自由完成“看账户 → 找报告 → 解释报告 → 纠正股票 → 换话题 → 返回报告”；验证对象和来源一致，不暗中创建任务。**已验收（2026-09-08）**：用户原话“能完整读取，且模型会自己对比”；环境为隔离 state 目录、worker 关闭、合成研究标记未出站，记录见股票仓库 `docs/计划.md`。

只把旧 `/api/master/broker/run` 包装成一个“回答股票问题”工具不能算完成此阶段。新总控应直接选择具体股票工具，避免把旧模型循环和回答格式整体嵌套回来。

IN2 联调与人工验收步骤（隔离环境，不动日常账本）：

1. 播种（后台停止时执行）：`cd D:/Stock_Agent_Workspace && ./.venv-paper/Scripts/python.exe scripts/seed_bridge_demo.py --conversation-id <本仓库会话 id>`。已播种演示会话为 `IN2 股票联调`（id `27c728138e284e42`，含 600150 HOLD 与 000001 REJECT 两个合成任务）。
2. 启动隔离后台：`scripts/run_paper.py --state-dir data/bridge-check-in22 --no-worker --port 8766`（worker 关闭；token 在该目录 `.paper-master-token`）。
3. 本仓库 `.env` 配置 `STOCK_BRIDGE_URL=http://127.0.0.1:8766` 与 `STOCK_BRIDGE_TOKEN=<上一步 token>`，然后用**唯一入口** `.\scripts\start-daily.ps1` 重启（2026-09-15 起不再有 IN1 专用入口；隔离靠独立股票状态目录与会话实现）。
4. 在 Web 打开“IN2 股票联调”会话自由测试；IN1 模型额度 2026-09-08 经用户要求调整为 120 次（IN1 用 30、IN2 联调 30、IN3 验收追加 60）。

### IN3：一条完整研究任务链

依赖：`IN2.H` 通过。复用股票后台队列、预算、幂等和检查点，不在本仓库新建第二套研究调度器。

1. `IN3.1`：接提交、查询、停止、恢复及结果读取，使用服务端可信任务引用；刷新和重试不重复创建任务。**已实现（2026-09-08）**：新增 `stock_research_submit`/`stock_research_cancel`/`stock_research_resume` 三个工具（契约见股票仓库工具网关 IN3 小节），桥接在 step 无观察时改用其脱敏 tool capsule 作为受理回执（QUEUED/CREATED/REUSED 对模型可见）。**标的标识（2026-09-09 实施）**：submit 改为“6 位代码或股票名称”二选一（`symbol` / `name`），名称由后台可信证券名称表解析成代码，模型不自行换算；实现与证据见第 6.2、6.3 节。
2. `IN3.2`：长任务排队后立即返回受理状态，让聊天继续。持久化任务与会话关联，完成结果可在后续对话或界面刷新时取得；状态通知不冒充已经读过报告。**已实现**：submit 入队即返回受理回执；进度/结果经 stock_research_find（含 unread）与 stock_report_read 在后续轮次取得；工具描述写明受理≠完成。
3. `IN3.3`：区分“停止生成”和“停止研究”。断开聊天流不自动取消研究；研究取消由独立工具请求指定任务，已发出模型调用的停止语义以后台实际能力为准。**已实现**：Web 停止按钮只断聊天流；研究取消仅经 stock_research_cancel（异步请求停止语义）；恢复走 stock_research_resume（服务端校验证据/版本/检查点，失败或结果不明的付费调用不重发）。

**IN3 隔离联调证据（2026-09-08，带 worker 的隔离后台，零 Qwen 调用）**：submit 600519 → QUEUED/CREATED → worker 真实执行（真实日线数据，技术 SUCCEEDED + 业务 HOLD/NEEDS_MATERIALS 未耗模型调用）；同标的重复提交被 reuse 未新建；find 显示 SUCCEEDED+unread；report_read 返回投影+F01 附件；resume 请求入队后被 worker 以“不适用局部续跑”明确拒绝；对已完成任务 cancel 返回友好说明。运行中任务的中途取消未实测（任务结束太快），该语义由股票侧既有回归覆盖。注意：带 worker 启动隔离后台会自动执行其日常管线（日监控 catchup + scan + 自动研究），属系统设计行为。

**模型自测暴露与修复（2026-09-08）**：让模型自行测试 cancel/resume 时一轮打了 8 次股票工具调用，撞到 broker 每轮 6 步预算；桥接在 STEP_LIMIT 后序号失步，后续调用被网关以 409 拒绝。已修复为边界即本轮终态、本地短路；并补充 submit 的 symbol/reference 二选一本地校验（2026-09-09 实施：submit 已改为 `symbol`/`name` 二选一，名称由股票侧可信名称表解析，见第 6.3 节第 1 条）。同期确认：服务端对"未出现在用户原话或本会话已知集合中的标的"拒绝研究（NEEDS_REFERENCE）是有意的防幻觉门禁，不是故障。
4. `IN3.H`：验证提交一次、跨轮查询、结果读取、失败说明、定向停止与检查点恢复。独立任务并行不混淆标的；有前置依赖的专业步骤保留顺序。**状态（2026-09-14）**：用户验收通过，原话“是的，我已经确认过 IN3.H 的实现了”；证据及验收范围见[运行记录](./历史/开发与验收记录-2026-09.md) §10。已登记缺陷继续单独跟踪。

先做离线/隔离联调，再按后续明确的模型调用预算进行真实验证；文档计划不是运行日常任务的授权。

### IN4：扩展专业能力与移除 Dify 依赖

依赖：首条研究链人工验收通过。按实际使用反馈接入其他职责和人工操作入口。

总控先独立于 Dify 对话入口，专业研究过渡期仍可调用后台 Dify 工作流。之后逐项替换专业模型适配器，保持输入材料、独立角色、输出契约、检查点和前置回执一致。启动器、操作入口和历史访问迁移单独验收；停用旧服务、删除应用和数据均不随代码接入自动发生。

**IN4 任务分组（2026-09-10 重整；编号与进度见股票仓库 `docs/计划.md`）**：

| 组 | 编号 | 内容 | 与本仓库关系 |
| --- | --- | --- | --- |
| 板块放开与研究可达性（**已完成**） | `IN4.17` | 板块白名单两处放开到创业板/科创板 + 板级涨跌幅；北交所无数据源故仍拒。锚定门禁经实测判定**保留**（名称路本就通） | 主体在股票仓库；本仓库 `core/stock_bridge.py` 接受任意 6 位代码，**无需改动** |
| 技能结构对齐 | `IN4.16` | 对齐 Agent Skills（SKILL.md）标准：frontmatter 解析与校验（`skills/runner/skill_md.py`）、`skill_list`/`skill_info`/`skill_run`/`skill_unregister` 双形态（渐进披露：清单无正文、info 给正文+资源清单+注入防护提示、run 给引导）、容器集成 26 项全过、wb-finance-skill 原样放入零改代码可用 | **本仓库为主**（`skills/runner/skill_registry.py`、`skill_md.py`、工具 YAML） |
| 原有范围 | `IN4.1`～`IN4.3` | 接其余约 16 个专业职责与人工入口；逐项替换 Dify 专业调用；日常切换与回退验证 | 三者验收均涉及本仓库（模型路由、工具面、启动器） |
| 资产迁移 | `IN4.4`～`IN4.7` | 外部技能资产盘点、46 份方法论 references、可移植分析代码、数据面评估 | 主体在股票仓库；本仓库只承接总控级输出规范 |
| 数据面 | `IN4.8`～`IN4.13` | 自建选股引擎、产业链近似、neodata 式取数、通达信本地数据、IC/IR（按需）、付费登记 | 白名单与取源在股票仓库；本仓库 skills 容器不参与取数 |
| 既有缺陷 | `IN4.14`、`IN4.15`、**`IN4.18`** | 行业口径、集中度预警实现形态；规则类拒绝须给原因（**`IN4.18` 已完成并经用户验收**） | 股票仓库；本仓库的账户与持仓卡片展示受其结论影响，`IN4.18` 的聊天侧改动在 `core/stock_bridge.py` |

实施顺序（2026-09-14 用户更新）为**先修 N2 并完善日常启动与 Web 控制（已完成并验收），再做规则拒绝原因 `IN4.18`（已完成并经用户验收），接着 `IN4.17` 板块放开与研究可达性（已完成并端到端实测）**，然后对齐技能结构（`IN4.16`），再修风控（`IN4.14`/`IN4.15`）、迁移准备、持股期能力、选股与取数、择时与独立，最后收口与兜底。`IN4.17` 的**锁一**（板块白名单 `jobs.py:42` 提交门禁 + `data.py:231` 候选池过滤，加板级涨跌幅）已解；**锁二**（锚定门禁）经实测判定**保留**——`_name_anchored` 锚定的是名称而非代码，名称出现在用户原话即通过，此前失败只因板块白名单，故"名称路"本就通；仅"用户明说先搜代码再研究"这一窄路径仍被拒，**刻意不放宽**以免实质上取消防幻觉闸门。**北交所仍不支持**（实测 `920185` 返回 0 根 K 线）。

**与本仓库直接相关的约束**（详见股票仓库 `docs/计划.md` 的「评估结论」）：① 该批技能是 `SKILL.md` 加 `references/` 与 `scripts/`，与本仓库 `/workspace/skills/*.py`（`SKILL_METADATA` + `run()`）契约不兼容，**但该形态正是 Agent Skills 开放标准（45+ 客户端采用），故先完成 `IN4.16` 即可按原格式直接消费；不先对齐才需逐份手工改写**；② 运行技能的 runner 容器是 `python:3.11-slim`（**无 Node**），且技能子进程环境被 `_SAFE_ENV` 固定、**不传递密钥**，宿主型技能无法移植；③ `neodata` 凭证需宿主工具 `connect_cloud_service`、`westock` CLI 本机未安装且源自腾讯专有发布源，数据通路不可直接获取；④ 该批 references 存在"文档虚标代码能力"（宣称因子脚本含 IC/IR 与分层回测，实际没有），迁移以代码为准；⑤ **免费优先、付费靠后**，付费项必须写明免费平替及其缺点。本批**不复制文件、不改代码、不安装依赖**。

`IN4.1`～`IN4.18` 各项"实现后能做到什么、在选股与持股各管哪一段、验证时看什么"的验收说明（含选股与持股两条流水线的责任划分），见股票仓库 `docs/计划.md` 的「IN4 实现后可以做到什么」小节，本文件不重复维护。

## 4. 模块归属与延期项

| 归属 | 内容 |
| --- | --- |
| 本仓库通用核心 | 模型适配、消息协议、工具执行循环、上下文与记忆、聊天 UI、通用工具结果展示 |
| 本仓库股票扩展 | 股票工具描述、后台连接配置、身份与请求绑定、股票结果卡片；使用独立模块，具体新目录在实施时确定 |
| 股票仓库 | 数据采集、量化、专业角色和工作流、研究队列、账本、策略规则、人工门禁、可信业务回执 |

专业角色每次只加载选定职责的提示词、材料和必要证据，不共享整段总控历史；共享模型供应商不等于共享角色记忆。股票账本事实不由通用聊天摘要代替，通用文件/代码工具不能成为绕过股票执行门禁的写入通道。

更多主题、更多供应商、多用户、复杂插件市场、统一调度平台等暂不作为接入前置。旧规划中已有功能继续保留，只有影响当前验收的缺口优先修复。

宿主机 Windows PowerShell/CMD、交互式 PTY/长期后台进程，以及基于 `SKILL.md` 方法说明的技能包列为后续扩展，均不作为 IN1 前置。本期保留 Python 可执行 skills；方法型技能包已立项为 `IN4.16`（对齐 Agent Skills 标准，最高优先，前置 `IN4.4`/`IN4.5`）与 `IN4.5`（方法论迁移），以后可与 Python skills 共存。Linux 容器工作区与 Windows 本机路径必须明确区分。

## 5. main 开发与记录约定

- 两仓库直接使用 `main`，不创建股票专用分支。每批开始检查已有改动；与其他 AI 协作时按文件和批次协调，不覆盖其工作。
- 通用聊天修复、股票适配、后台改造分成可审阅的小提交，即使同在 `main` 也不要混成一次大迁移。
- 每批同步统一进度表：任务编号、涉及仓库、实际改动、离线检查、真实联调、人工验收、提交编号和剩余缺口。未完成或未验证的项目保持未勾选。
- 两仓库提交无法原子完成；涉及接口变更时记录契约版本与配套提交，并保持过渡兼容。提交代码不代表日常入口已经切换。
- 工程进度（截至 2026-09-14）：IN1.1～IN1.7、IN2.1～IN2.3、IN3.1～IN3.3 已实现；IN1.H、IN2.H、IN3.H 均经用户验收通过。N2 本批与关闭状态修复已验收通过；**`IN4.18`（规则类拒绝给出原因）已实现并经用户验收**——股票侧新增 `REJECTION_CODES` 白名单随 `reason_code`/`guidance` 回传，本仓库 `core/stock_bridge.py` 优先透出该原因。**`IN4.17`（板块放开与研究可达性）已完成**：股票侧放开创业板/科创板白名单并按板块计算涨跌停，宁德时代 `300750`、中芯国际 `688981` 研究任务端到端 `SUCCEEDED`；本仓库无需改动（`_SYMBOL_RE` 本就接受 6 位代码）。**`IN4.16`（技能结构对齐）已于 2026-09-15 实现并实测通过**（frontmatter 解析校验、双形态发现与渐进披露、`wb-finance-skill` 原样放入零改代码可用，待人工验收）；**下一批 `IN4.14`/`IN4.15`（集中度风控缺陷）**。

## 6. 当前状态与待办（2026-09-10 更新）

### 6.1 工程状态

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| IN1.1～IN1.7 | ✅ 已实现 | 聊天底座：模型路由与真实来源、完整工具协议落库、异常与停止收尾、自然对话与云端边界、skills 发现/调用/注册、精确编辑、容器内命令执行。 |
| IN1.H | ✅ 已验收 | 2026-09-07 用户经验性验收通过并明确进入 IN2；44 个必测案例矩阵与第二段 12 轮对话未逐条全量执行。 |
| IN2.1 | ✅ 已冻结 | 只读工具契约与名称已冻结（2026-09-07）：`stock_account_view`、`stock_research_find`、`stock_report_read`。 |
| IN2.2 | ✅ 已实现 | `core/stock_bridge.py` 按轮 prepare/step/finish；票据、序号、request_id 不出适配层；未配置 `STOCK_BRIDGE_URL` 时股票工具不注册。 |
| IN2.3 | ✅ 已实现 | 账户/研究/报告卡片、证据时间与本地报告正文展示；完整报告不进入模型历史，泄漏边界已有离线测试固化。 |
| IN2.H | ✅ 已验收 | 2026-09-08 用户验收通过（原话“能完整读取，且模型会自己对比”）；隔离 state 目录、worker 关闭、合成标记未出站。 |
| IN3.1 | ✅ 已实现 | 新增 `stock_research_submit` / `stock_research_cancel` / `stock_research_resume`。 |
| IN3.2 | ✅ 已实现 | submit 入队即返回受理回执；进度和结果通过 `stock_research_find` / `stock_report_read` 在后续轮次取得。 |
| IN3.3 | ✅ 已实现 | Web“停止生成”只断聊天流；研究取消仅走 `stock_research_cancel`；恢复走 `stock_research_resume`。 |
| IN3.H | ✅ 用户验收通过（2026-09-14） | 真实双任务推进、跨轮读取与对比证据见运行记录 §9；已知缺陷继续跟踪。 |
| IN4 | 🔄 进行中 | **`IN4.18`（规则类拒绝须给原因）已完成并经用户验收**；**`IN4.17`（板块放开与研究可达性）已完成并端到端实测（均为 2026-09-14）**；**`IN4.16`（技能结构对齐）已完成并实测通过（2026-09-15，待人工验收），下一批 `IN4.14`/`IN4.15`**。2026-09-10 重整为五组（`IN4.1`～`IN4.18`）：技能结构对齐、原有范围 `IN4.1`～`IN4.3`、资产迁移 `IN4.4`～`IN4.7`、数据面 `IN4.8`～`IN4.13`、既有缺陷 `IN4.14`/`IN4.15`/`IN4.17`/`IN4.18`；见 6.3 第 8 条。实施顺序见第 3 节 IN4 分组表。 |

### 6.2 运行与联调证据

- **隔离联调（2026-09-08，带 worker、零 Qwen 调用）**：submit 600519 → QUEUED/CREATED → worker 真实执行；同标的重复提交被 reuse；find 显示 SUCCEEDED+unread；report_read 返回投影与 F01 附件；resume 被明确拒绝；已完成任务 cancel 返回友好说明。详见第 3 节 IN3。
- **模型在环调用记录**：`data/in1/logs/audit.jsonl` 中会话 `27c728138e284e42` 在 2026-09-07～2026-09-08 实际调用过账户查看、研究查找、报告读取、提交、取消和恢复；最后一条为 2026-09-08 07:27 UTC（`stock_research_find` 报错）。其中两次 `redo` 提交同时带了 `symbol` 与 `reference`（06:16、07:26 UTC），正是第 6.3 节第 1 条描述的冲突口径。模型调用账本 `data/in1-model-calls.sqlite` 为 **70/120**，此后无新增调用。
- **离线复核（2026-09-09 本机）**：聊天侧 `unittest discover -s tests` 86 项执行、85 通过 + 1 项容器门控 skip；`apps/web` 生产构建通过。容器集成检查与真实模型对话本轮均未重跑。
- **名称标识联调（2026-09-09）**：股票侧新增可信证券名称表与名称解析（提交 `ae91b53`），聊天侧 submit 改为 `symbol`/`name` 二选一（提交 `28789dd`）。隔离真实后台实测：query「帮我研究一下中国船舶」+ `reference.candidate_name=中国船舶` → 解析为 `600150` 并命中已有任务（REUSED，返回任务/裁决/数据质量投影）；名称不在用户本轮消息时返回 `NEEDS_REFERENCE` 并记录待补标的。测试：股票侧 197 项 pytest 通过（含 7 项新增名称解析用例）、聊天侧 86 项离线测试通过（1 skip）。
- **IN3 模型在环复验（2026-09-09，真实 Qwen qwen3.5-flash，隔离后台 worker 关闭，会话 `911b922351d142d8`）**：脚本 `scripts/check_in3_model_loop.py`，账本 70 → 93（本轮消耗 23 次，含一次因探针编码问题重跑的回合）。六项检查全部通过：① 按名称复用研究——「中国船舶」→ `600150`，`REUSED` 并返回投影；② 按名称新建研究——「中国石油」→ `601857`，`QUEUED/CREATED` 受理回执，模型回答明确"已排队、稍后问进度"，未冒充完成；③ 跨轮 `stock_research_find` 列出本会话 3 个任务；④ `stock_report_read` 交付完整本地报告（附件含 `SYNTHETIC_DEMO_NOT_FOR_CLOUD` 标记，模型回答不含该标记）；⑤ 定向停止与恢复——`stock_research_cancel` 成功、`stock_research_resume` 按设计拒绝，模型如实说明两者结果；⑥ 步数边界回归——一轮 8 次股票调用，第 7、8 次返回终态 `STEP_LIMIT`（"步数或时间已达上限，已取得的结果保留"），该轮仍正常结束并交付最终回答，未出现 409 序号失步。原始事件见 `data/in3-model-loop.json` 与 `data/in3-model-loop-cancel.json`。
- **当前运行配置（2026-09-14 更新，环境已于 2026-09-15 退出）**：`.env` 指向隔离演示后台 `STOCK_BRIDGE_URL=http://127.0.0.1:8766`。**当时的验收环境**：Docker Desktop、聊天侧技能容器（隔离栈 19101/19102/19103，`compose.in1.yml`）、聊天 BFF 9510、Web 5173、股票隔离后台 8766（`data/bridge-check-in22`，**worker 已开**，模式已解除至 `ACTIVE`）。隔离后台可用股票仓库 `scripts/start_paper_acceptance.ps1` 自行启停（`-Worker` 开 worker、`-Stop` 停止、`-Status` 查状态）——**与日常后台（8765 / `simulation\state.sqlite`）完全隔离，不受影响**。2026-09-15 单一入口改造后，上述隔离栈与专用入口已全部移除：日常入口为 `启动.bat`/`scripts/start-daily.ps1`（9510 同源托管 Web，工具容器 9101/9102/9103），验收隔离改用日常入口 + 独立股票状态目录实现，IN1 会话库已并入 `data/conversations.db`。

### 6.3 已知缺口

1. ✅ **`stock_research_submit` 的 reference 语义三方不一致（2026-09-09 已解决，聊天侧 `28789dd`、股票侧 `ae91b53`）**：submit 的标的标识改为“6 位代码或股票名称”二选一。聊天侧 YAML 暴露 `symbol` / `name`（同时给出或都不给会被本地拒绝），桥接把 `name` 原样映射为 `{'type':'candidate_name','name':…}`；股票侧先查本会话候选表，未命中再查可信证券名称表（`data/code_names.json`，scan 任务自动刷新，也可用 `scripts/refresh_code_names.py` 手动刷新），并保留锚定门禁——名称必须出现在用户本轮消息或本会话已知标的中。submit 不再接受 64 位任务引用；cancel / resume / report_read 仍按任务引用。待办：正式验收时确认名称表的刷新频率是否足够（当前随 scan 刷新）。
2. ✅ **边界修复后的模型在环回归（2026-09-09 已复验）**：修复 `f0b8a64`（步数/时间边界即本轮终态、submit 目标校验）原只有离线测试覆盖；本轮真实模型在环复验中，一轮 8 次股票调用触发两次终态 `STEP_LIMIT`，该轮正常结束并交付最终回答，未出现序号失步或 409，见第 6.2 节。IN3.H 的工程前置至此全部完成；人工验收于 2026-09-14 通过。
3. ✅ **IN3.H 人工验收通过（2026-09-14）**：用户原话“**是的，我已经确认过 IN3.H 的实现了**”（在复核本轮全部改动后给出）；对话证据与范围见[运行记录](./历史/开发与验收记录-2026-09.md) §10。N2 启动死结已在后续本批修复并验收，IN4 已知缺陷仍保留。
4. **文档同步**：2026-09-09 已修正本仓库的验收状态、模型额度、工具分层、轮次上限、股票工具数量与若干界面描述（复核记录见 [开发与验收历史记录](./历史/开发与验收记录-2026-09.md) §8），并按第 1 条实施了名称标识；2026-09-10 按 IN4 重整结果同步了本文件的第 1、3、4、5、6 节与两个股票侧文档，并重写部署指南与运行记录（后者按实测更正了测试项数与 `test_stock_bridge` 计数）。跨仓库仍有文档债：① 股票仓库 `docs/计划.md` 的批次记录表历史行在 2026-09-06 之前不完整（2026-09-07～10 已补）；② “带 worker 启动隔离后台会自动执行日常管线”只有本侧文档记录，股票侧文档未记（`NEEDS_REFERENCE` 门禁已在股票侧工具网关补记）。
5. **仍未覆盖的验证**：容器集成检查（18 项）未重跑——**2026-09-14 起 Docker 已在运行、该项已具备重跑条件**，但本轮尚未执行。模型在环已覆盖名称提交、跨轮查询、报告读取、定向停止/恢复与步数边界（见 6.2）；IN3.H 已由用户确认通过，新增双任务推进证据见运行记录 §9；未逐项留证的场景不追认为本轮已重测。
6. **集中度风控的两项既有缺陷（股票侧，2026-09-10 评估发现）**：`IN4.14` 行业上限建立在自由文本行业名上（`ledger.py:138` 按字符串精确相等归并 + `data.py:167` 双供应商标签不一致 + `ledger.py:222` 买入写死），可被静默绕过；`IN4.15` `B03_集中度预警` 规格要求"本地规则工具、不配置专属大模型、输入申万行业分类"，实现却是 `gen_dsl.py:366` 的 LLM 节点。**两者叠加会给出"假安全"结论**。本仓库的账户与持仓卡片会展示其结论，验收持仓相关回答时须一并核对。修复登记在股票仓库 `docs/计划.md`，排在 IN4 实施顺序最前。
7. **历史缺陷（已在 N2 本批修复并验收，见 §6.4）：`RECOVERING` 无法自解，且停开超过一个交易日必然进入该状态（股票侧，2026-09-14 验收期间实测复现；根因属 N2）**：`Control.startup()` 在缺口 > 1 个交易日时置 `RECOVERING`，但 worker 的 `schedule()` 非 `ACTIVE` 时直接返回故**不补跑**，而唯一的恢复路径 `RESUME` 又被 `resume_problems()` 的 `gap > 1` 拒绝——**触发门禁的原因同时是出路被堵的原因**。验收时的表现为聊天侧提交研究收到 **`RULE_BLOCKED`**，容易被误判成 worker 未启动。**日常库同样适用**：股票后台停开超过一个交易日即掉入同一死结，只能人工干预。详见股票仓库 `docs/计划.md`「IN3 期间发现的既有缺陷」。**判定提示**：出现 `RULE_BLOCKED` 时先查 `runtime.mode`，不要先查 worker。
8. ✅ **`IN4.18` 已修复并经用户验收（2026-09-14）**：股票侧新增显式白名单 `REJECTION_CODES` 并随 `reason_code`/`guidance` 回传，本仓库 `core/stock_bridge.py` 优先透出该原因，未分类时回退为"本机未提供可转述的原因"而不再诱导改参数。**验收证据**：用户输入「研究 300750」→ 工具返回 `UNSUPPORTED_BOARD` 与「该股票所属板块暂不支持（首版仅支持沪深主板）」，助手据此复述"不是权限问题，是板块限制"，并自行摸清前一次失败原因是**提问里没带代码**（锚定门禁）。
9. ✅ **`IN4.17` 已完成：板块放开与研究可达性（2026-09-14）**。**锁一已解**——`symbol()` 放开到创业板/科创板（`300`/`301`/`302`/`688`/`689`），且按"不能只改正则"的要求同步了下游：`ledger.py` 原把涨跌停**硬编码 ±10%**（创业板/科创板为 ±20%，会静默拒绝合法成交），已改为板级取值；`data.py` 的 `board` 字段由硬编码 `"MAIN"` 改为按代码判定；成交量口径实测各板块一致、无需特判；`operator.py` 的用户可见文案同步。**锁二经实测判定保留门禁**——`master.py:1198` 锚定的是**名称**而非代码，名称出现在用户原话即通过；此前失败只因板块白名单在解析成功后拒绝了代码，故"名称路"本就通，**无需放宽防幻觉闸门**。**实测**：按名称「宁德时代」→ `CREATED`/`QUEUED`，symbol `300750`；按代码「300750」→ `REUSED`；按名称「中芯国际」→ `CREATED`，symbol `688981`；三份研究任务（含主板 `600150`）均 `SUCCEEDED`，宁德时代结果为真实数据（收盘 337.11、729 根 K 线、净利率 16.98%、负债率 63.65%），缺口如实标注「公司行动来源不可用」。**北交所仍不支持**（实测 `920185` 返回 0 根 K 线），但拒绝理由已从"仅支持沪深主板"改为准确表述。**残留**：创业板/科创板公司行动来源不可用，故这些标的的**订单会被 `eligibility` 拦为待核查**（只读研究不受影响）；"用户明说先搜代码再研究"这一窄路径仍被锚定门禁拒，已列为已知限制。


### 6.4 N2 恢复与日常 Web 控制交付（2026-09-14）

已实现独立股票进程管理、worker 开关、恢复进度与可信人工入口；新增 `scripts/start-daily.ps1`。股票侧新增持久化逐日恢复、历史账本重建、有限重试、原模型回执复用与恢复前后备份。第 6.3 节第 7 条记录的启动死结已有执行路径，不再依赖人工改库。股票服务不启用时不注册股票工具，后台关闭不影响聊天。每日操作见[日常运行指南](./日常运行指南.md)，测试步骤见[回归验证指南](./回归验证指南.md) §7，恢复实现与历史证据边界见[历史记录](./历史/开发与验收记录-2026-09.md) §11。N2.H 已于 2026-09-14 用户验收通过，包括关闭后台状态修复后的复测（`a1ba270`）。原 N2 扩大能力边界保留；**其后 `IN4.18` 已经用户验收、`IN4.17` 已完成并端到端实测（宁德时代 `300750` 与中芯国际 `688981` 研究任务 `SUCCEEDED`），IN4.16 已于 2026-09-15 实现并实测通过（待人工验收），下一批 `IN4.14`/`IN4.15`。**
