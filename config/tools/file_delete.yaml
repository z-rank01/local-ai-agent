name: file_delete
backend: skill-files
tier: extended
short_description: "Delete a file."
description: "Move a file to trash (soft delete, reversible). Cannot delete files in /workspace/skills/."
parameters:
  type: object
  properties:
    path:
      type: string
      description: "Absolute path starting with /workspace"
  required: ["path"]
