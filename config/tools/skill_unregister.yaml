name: skill_unregister
backend: skill-runner
description: |
  删除一个已注册的技能。移除技能脚本文件和配置文件。
parameters:
  type: object
  properties:
    skill_name:
      type: string
      description: "要删除的技能名（不含 .py 后缀）"
  required:
    - skill_name
