name: file_read
backend: skill-files
tier: core
short_description: "Read a file."
description: "Read the contents of a file in the workspace."
parameters:
  type: object
  properties:
    path:
      type: string
      description: "Absolute path starting with /workspace"
  required: ["path"]
